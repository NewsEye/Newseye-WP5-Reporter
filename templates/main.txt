en: there were {result_value} entries {result_key}
| analysis_type = facet_count

en: there were {result_value} entries related to {result_key}
| analysis_type = topic_count

en-head: Analysis of a corpus defined by the query {corpus}
| corpus_type = query_result